# Optional compatibility check: LexiDT objective plus OpenAI-calibrated SemDT.
name: combined
base_recipe: kuleshov_var
differential_objective: lexi
semantic_constraint: nli
semantic_threshold:
  source: calibrated
  backend: openai
  # Null resolves to calibration.output_root/<backend>/threshold.json.
  file: null
comparison_baseline: base
